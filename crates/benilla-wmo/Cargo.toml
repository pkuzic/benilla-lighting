[package]
name = "benilla-wmo"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WMO (root and group) reader for WoW 1.12.1 (build 5875)"

[lints]
workspace = true

[dependencies]
benilla-bytes = { path = "../benilla-bytes" }
